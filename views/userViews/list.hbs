<script>0
    $(document).ready(function(e){
        $(".img-check").click(function(){
            $(this).toggleClass("check");
            $(this).find("#checkbox").attr('checked');
        });
    });

    $(document).ready(function(){
        $(".occupation").click(function(){
            let occupation = $(this).text().trim();
            $(".row").children().hide();
            $(".row").find("p:contains(" + occupation + ")").parent().parent().parent().show();

        })
    });
</script>

<main>
    {{#if user}}
    <!--  Body  -->
    <div class="container">

        <!-- Left Section -->
        <div class="container col-md-3 section">
            <div class="list-group">
                <div class="list-group-item active">
                    Filter by Profession
                </div>
                <div class="list-group-item">
                    <a href="" onclick="return false" id="filter" class="occupation">
                        All</a> <span class="badge">{{users.count}}</span>
                </div>
                {{#each professionsCount}}
                    <div class="list-group-item">
                        <a href="" onclick="return false" id="filter" class="occupation">
                            {{this.profession}}</a>s <span class="badge">{{this.count}}</span>
                    </div>
                {{/each}}
            </div>
        </div>

        <!-- Right Section -->
        <div class="section col-md-9">
            <h3>User Profiles</h3>
            <hr>

            <!-- Filters and Stuff -->
            <div class="filters-container">
                <div class="input-group input-group-lg">
                    <label class="sr-only" for="myInputActive"></label>
                    <input class="form-control" type="text" id="myInputActive" onkeyup="myFunction('activeProjectsTable', 'myInputActive')" placeholder="Find User">
                </div>
            </div>

            <!--  Users Check -->
<!--            <div class="row">
                <form method="post" action="/team/create/createNew">
                    {{#each users}}
                        <div class="form-group">
                            <div class="col-lg-4 col-md-4 col-sm-6 col-xs-6">
                                <div class="thumbnail img-check">
                                    <img class="img-thumbnail" src="/images/lora.jpg">
                                    <div class="caption">
                                        <h5 class="text-center">{{this.fullName}}</h5>
                                        <p class="text-center">{{this.profession}}Designer</p>
                                    </div>

                                    <input type="checkbox" id="checkbox" name="users" value="{{this.id}}" hidden>
                                </div>
                            </div>
                        </div>

                    {{/each}}
                    <div class="col-xs-12">
                        <input type="submit" value="Create Team" class="btn btn-success pull-right">
                    </div>
                </form>
            </div>-->

            <!--  All Users List  -->
            <div class="row">
                {{#each users}}
                    <div class="col-lg-4 col-md-4 col-sm-4 col-xs-6 profileCard">
                        <div class="thumbnail">

                            <img class="img-tumbnails" src="/images/userProfilePictures/{{this.picture}}">
                            <div class="caption">
                                <!--  Layer Over  -->
                                <a href="/userViews/details/{{this.id}}">
                                    <div class="thumbnail-over">
                                        <p>View Profile</p>
                                    </div>
                                </a>
                                <h6 class="text-center">{{this.fullName}}</h6>
                                <p class="text-center">{{this.occupation.occupationName}}</p>
                            </div>
                        </div>
                    </div>
                {{/each}}
            </div>
        </div>
    </div>
    {{/if}}
</main>